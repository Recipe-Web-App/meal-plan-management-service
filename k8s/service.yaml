---
# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: meal-plan-management-service
  namespace: meal-plan-management
  labels:
    app: meal-plan-management-service
spec:
  selector:
    app: meal-plan-management-service
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
